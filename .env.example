# LPTV 后端环境变量示例
# 注意：proxy-server.cjs 不自动加载 .env（无 dotenv 依赖）
# LPK 部署：环境变量由懒猫云平台注入，本文件仅供单机部署时参考
# 本地开发：npm run dev 已内置默认值（PORT=3000 / localhost:5173）

# 后端端口（默认 3000）
# PORT=3000

# 前端允许的 Origin 白名单（逗号分隔）
# 默认已含 http://localhost:5173,http://127.0.0.1:5173（开发用）
# 生产环境请显式设置：
# ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# 运行环境（非 production 时后端放行所有 Origin）
# NODE_ENV=production